import { type Course } from '../types/course.type';

type Properties = {
  courses: Course[];
}

const CoursesPage: React.FC<Properties> = ({courses}) => {
  return (
    <div>

    </div>
  )
}

export { CoursesPage };